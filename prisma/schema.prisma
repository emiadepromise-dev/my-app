generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model ScanHistory {
  id         String   @id @default(cuid())
  type       String
  target     String
  summary    String
  resultData String
  createdAt  DateTime @default(now())
}
