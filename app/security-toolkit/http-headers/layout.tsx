import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "HTTP Header Checker | CyberYoshi",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
