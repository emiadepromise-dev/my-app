import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "File Hashing | CyberYoshi",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
